<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Box Buddy</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link rel="icon" href="/images/icon-black.png">

    <style>
        :root {
          --color-custom: rgb(238, 62, 56);
          --color-custom2: rgb(238, 62, 56, 0.9);
        }
        .brand-red {
          background-color: var(--color-custom);
          /* Additional styles for the custom element */
        }
        .custom-red {
          border-color: var(--color-custom2);
          /* Additional styles for the custom element */
        }
    </style>

</head>
<body>

    <div class="flex flex-col h-screen bg-white overflow-auto"> 

        <!-- Navbar -->
        <%- include('./partials/navbar') %>
    
        <!-- Main content -->
        <div class="flex flex-row">
            <div class="flex flex-col my-14 ">

                <form action="/login" method="POST" class="mb-10 flex flex-col w-screen justify-evenly items-start text-lg my-auto px-1 mt-14">
                    <div class="text-2xl font-bold mb-10 mx-auto">
                        User Login:
                    </div>
                    <div class="ftext-lg mb-10 ml-6 font-semibold">
                      <label for="email">Email:</label>
                      <input type="email" name="email" id="email" placeholder="Email address">
                    </div>
                    <div class="mb-10 text-lg ml-6 font-semibold">
                      <label for="password">Password:</label>
                      <input type="password" name="password" id="password" placeholder="***********">
                    </div>
                    <button name="login" id="login" class="bg-black text-white font-bold py-2 px-4 rounded mx-auto" type="submit">
                        Log In
                    </button>
                  </form>

                  <div class="mb-5 mx-auto text-center">
                    Don't have an account? <br>Register below:
                  </div>

                  <a href="/registration" class="mx-auto">
                    <button id="registration" class="bg-black text-white font-bold py-2 px-4 rounded mx-auto">
                      Register
                  </button>
                  </a>

            </div>

        </div>
    
        <!-- Footer -->
        <%- include('./partials/footer') %>
    



    </div>

</body>
</html>